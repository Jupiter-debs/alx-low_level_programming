#include <stdio.h>
/**
 * main - entry point
 *
 * Return:  0 if success
 */

int main(void)
{
	int i = 48;

	while (i <= 56)
	{
		putchar(i);
		putchar(', ');
		i++;
	}
	putchar(57);
	putcar(32);
	return (0);
}
