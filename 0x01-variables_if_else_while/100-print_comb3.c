#include <stdio.h>
/**
 * main - entry point
 *
 * Return:  0 if success
 */

int main(void)
{
	int i = 0;
	int j = 1;

	for (i = 0; i <= 9; i++)
	{
		for (j = 1; j <= 9; j++)
		{
			if (ij == ji)
			{
				continue;
			}
			if (i == j && j == i)
			{
				continue;
			}
			if (ij > ji)
			{
				continue;
			}
			putchar (i);
			putchar(j);
			putchar(44);
			putchar(32);
			i++;
			j++;
		}
	}
	return (0);
