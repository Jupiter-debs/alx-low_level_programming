#include <stdio.h>
/**
 * main - funtion to run put
 *
 * Return: returns (0) when successful
 */
int main(void)
{
	/* this displays the text in the put function */
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
